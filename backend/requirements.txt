# 智翼平台 — 后端依赖
# 对应 pyproject.toml，供 pip install -r requirements.txt 使用

# Web Framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29

# Database
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29
alembic>=1.13

# Validation
pydantic>=2.6
pydantic-settings>=2.2

# Auth
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.1

# File Upload
python-multipart>=0.0.9

# Cache & Queue
redis>=5.0
celery>=5.4

# SSE Streaming
sse-starlette>=1.8

# Logging
loguru>=0.7

# Async File I/O
aiofiles>=24.1

# HTTP Client (for LLM calls)
httpx>=0.27

# Env
python-dotenv>=1.0

# Dev
pytest>=8.1
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.4
black>=24.4

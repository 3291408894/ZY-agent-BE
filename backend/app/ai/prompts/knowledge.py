"""知识图谱生成 Prompt 模板 (PBI_11)"""

# ═══════════════════════════════════════════════════════════
# 系统角色设定
# ═══════════════════════════════════════════════════════════

SYSTEM_PROMPT = """你是一位资深的K12教育知识图谱专家，擅长梳理学科知识体系并构建结构化的知识图谱。
你的知识图谱需要做到：
1. 层级清晰：学科 → 单元/模块 → 知识点，层次分明
2. 关系准确：知识点之间的关联符合教学逻辑
3. 覆盖全面：涵盖核心概念、重点考点、常见易错点
4. 坐标合理：为每个节点分配合适的 x/y 坐标（画布 1200x900），使图谱美观可读

请始终以严格的 JSON 格式输出图谱数据。"""

# ═══════════════════════════════════════════════════════════
# 按学科/章节生成知识图谱
# ═══════════════════════════════════════════════════════════

SUBJECT_GRAPH_TEMPLATE = """请为以下学科内容构建知识图谱：

### 学科信息
- 学科：{subject}
- 范围：{source}

请生成一个层级化的知识图谱，包含以下类型的节点：

1. **category（分类节点）**：学科大类或模块，如"基础知识"、"阅读理解"、"写作"、数学的"代数"/"几何"等
2. **article（课文/章节节点）**：具体的课文篇目或教材章节
3. **knowledge（知识点节点）**：具体的知识点，如"通假字"、"比喻修辞"、"一元二次方程"等

**节点数量建议**：总共 15-30 个节点
- 1-2 个 category 节点
- 3-8 个 article/章节 节点（取决于范围）
- 10-20 个 knowledge 节点

**坐标布局规则**：
- category 节点放在顶部区域（y: 30-100）
- article 节点放在中部区域（y: 150-400）
- knowledge 节点放在下部区域（y: 450-800）
- 同一层级的节点在 x 轴上均匀分布（x: 50-1150）
- 画布尺寸：1200 × 900

**边的关系类型**（relation 字段）：
- category → article：用 "包含"
- article → knowledge：用 "重点考查"
- knowledge → knowledge：用 "关联" / "前置知识" / "延伸"
- category → knowledge：用 "涵盖"

请严格按以下 JSON 格式输出（只输出 JSON，不要其他文字）：

```json
{{
  "title": "图谱标题（如：七年级语文文言文知识图谱）",
  "nodes": [
    {{"id": "n1", "label": "文言文基础", "type": "category", "x": 600, "y": 50}},
    {{"id": "n2", "label": "桃花源记", "type": "article", "x": 200, "y": 250}},
    {{"id": "n3", "label": "实词释义", "type": "knowledge", "x": 100, "y": 500}}
  ],
  "edges": [
    {{"source": "n1", "target": "n2", "relation": "包含"}},
    {{"source": "n2", "target": "n3", "relation": "重点考查"}}
  ]
}}
```"""

# ═══════════════════════════════════════════════════════════
# 按文件内容生成知识图谱
# ═══════════════════════════════════════════════════════════

FILE_GRAPH_TEMPLATE = """请根据以下文件解析出的文本内容，构建知识图谱：

### 文件内容
{content}

请识别文本中涉及的知识点，并构建结构化的知识图谱。包含以下类型的节点：

1. **category（分类节点）**：知识大类
2. **article（课文/章节节点）**：涉及的具体篇目或章节
3. **knowledge（知识点节点）**：具体的知识点

**坐标布局规则**：
- category 节点放在顶部（y: 30-100）
- article 节点放在中部（y: 150-400）
- knowledge 节点放在下部（y: 450-800）
- 画布尺寸：1200 × 900

请严格按以下 JSON 格式输出（只输出 JSON）：

```json
{{
  "title": "图谱标题",
  "nodes": [
    {{"id": "n1", "label": "节点标签", "type": "category|article|knowledge", "x": 600, "y": 50}}
  ],
  "edges": [
    {{"source": "n1", "target": "n2", "relation": "关系描述"}}
  ]
}}
```"""

# ═══════════════════════════════════════════════════════════
# 节点详情 — 知识点深入分析
# ═══════════════════════════════════════════════════════════

NODE_DETAIL_TEMPLATE = """请为以下知识点提供详细的教学分析：

### 知识点
- 名称：{node_label}
- 所属学科/范围：{context}

请从以下四个方面进行分析，以 JSON 格式返回：

1. **summary**：知识点概述（2-3 句话，说清这个知识点的核心内容和重要性）
2. **key_points**：核心要点列表（3-5 个关键学习要点）
3. **example_questions**：典型例题（2-3 道，每道包含题目和答案）
4. **common_mistakes**：常见易错点（2-3 个学生容易出错的地方及纠正建议）
5. **related_knowledge**：关联知识点列表（3-5 个相关的知识点名称）

请严格按以下 JSON 格式输出（只输出 JSON）：

```json
{{
  "summary": "知识点概述文字...",
  "key_points": ["要点1", "要点2", "要点3"],
  "example_questions": [
    {{"question": "题目内容", "answer": "参考答案"}}
  ],
  "common_mistakes": [
    {{"mistake": "常见错误描述", "correction": "纠正建议"}}
  ],
  "related_knowledge": ["关联知识点1", "关联知识点2"]
}}
```"""

# ═══════════════════════════════════════════════════════════
# 图谱标题提取
# ═══════════════════════════════════════════════════════════

TITLE_GENERATION_TEMPLATE = """请根据以下信息，为知识图谱生成一个简洁、准确的标题（10-20 字）：

- 学科：{subject}
- 范围/来源：{source}
- 来源类型：{source_type}

只返回标题文本，不要加引号或额外说明。"""
